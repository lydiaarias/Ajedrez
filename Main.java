/**
 * Punto de entrada principal del programa.
 * Inicia la aplicación de ajedrez creando una instancia del Controlador.
 */
package Main;

import Controlador.Controlador;


public class Main
{
/**
 * Método principal que inicia la ejecución del programa.
 *
 * @param args Argumentos de línea de comandos (no se utilizan en este contexto).
 */
    public static void main(String[] args)
    {
        // Crea una instancia del Controlador para iniciar la aplicación de ajedrez
        Controlador controlador = new Controlador();
    }
}
